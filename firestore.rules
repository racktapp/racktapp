rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Default allow read/write access for all documents for any signed-in user.
    // This is insecure for production but will resolve permission errors for debugging.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
